@model IEnumerable<BrandsModel>

@foreach (var item in Model)
{
	<div class="brands-name">
		<ul class="nav nav-pills nav-stacked">
			<li><a asp-action="Index" asp-controller="Brands" asp-route-Slug="@item.Slug"> <span class="pull-right">(150)</span>@item.Name</a></li>
		</ul>
	</div>
}